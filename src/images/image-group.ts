// import {Palette} from './palette'

// A proxy for accessing images. Relative read / write operations affecting the group are cheap,
// reading the resolved value is expensive?
// export interface ImageGroup {
//   // but scaling, relative offset, size, etc all have to be calculated for any kind of cpu side calc
//   target?: XY
//   palette?: Palette
//   images: Image[]
// }
