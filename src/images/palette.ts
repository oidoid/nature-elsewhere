const size: number = 8
export enum Palette {
  GREYS = 0 * size,
  GREENS = 1 * size,
  BLUES = 2 * size,
  TREE = 3 * size,
  FROG = 4 * size,
  SNAKE = 5 * size,
  CATTAILS = 6 * size
}

export enum Tone {
  HALF = 1
  // todo: other colors
}

// todo: don't need a 256 tall grid palettes.png
