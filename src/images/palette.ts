const bankSize: number = 8
export enum Palette {
  GREYS = 0 * bankSize,
  GREENS = 1 * bankSize,
  BLUES = 2 * bankSize,
  TREE = 3 * bankSize,
  FROG = 4 * bankSize,
  SNAKE = 5 * bankSize,
  CATTAILS = 6 * bankSize,
  YELLOWS = 7 * bankSize,
  GRASS_GREENS = 8 * bankSize
}

export enum Tone {
  HALF = 1
  // todo: other colors
}
