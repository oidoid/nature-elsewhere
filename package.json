{
  "name": "nature-elsewhere",
  "author": "niedzielski",
  "version": "0.0.0",
  "description": "",
  "keywords": [],
  "homepage": "https://github.com/rndmem/nature-elsewhere",
  "repository": "github:rndmem/nature-elsewhere",
  "bugs": "https://github.com/rndmem/nature-elsewhere/issues",
  "license": "GPL-3.0",
  "scripts": {
    "start": "live-server -q src& tsc -w& wait",
    "test": "npm run -s formatter -- -l && tsc && jest",
    "format": "npm -s run formatter -- --write",
    "formatter": "prettier './**/*.{json,md,js,ts}'",
    "build:textures": "cd src/assets && aseprite -b ../../assets/*.aseprite --filename-format '{tag} {frame}' --list-tags --list-slices --data atlas.js --sheet-pack --inner-padding 1 --sheet atlas.png",
    "palettize": "cd assets && find -name \\*.aseprite -exec aseprite '{}' -b --palette palette.aseprite --save-as '{}' \\;"
  },
  "devDependencies": {
    "@types/jest": "23.3.9",
    "@types/webgl2": "0.0.4",
    "babel-preset-env": "1.7.0",
    "jest": "23.6.0",
    "live-server": "1.2.0",
    "pre-commit": "1.2.2",
    "prettier": "1.14.3",
    "typescript": "3.1.6"
  }
}
